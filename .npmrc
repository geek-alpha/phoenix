# npm 走国内镜像：package-lock.json 的 resolved 本来就指向 npmmirror，但安装不在
# lock 里的新包时会用 npm 默认源（registry.npmjs.org，国内很慢），这里补上。
# 曾经这里写的是 noproxy=* + strict-ssl=false，那是本机调试的绕法，且会关掉 npm 的
# TLS 证书校验、强制所有请求绕过代理——发行包带出去对用户是坑，已删。
registry=https://registry.npmmirror.com
