# 兼容壳 —— 真正的内容已统一到 requirements.txt。
#
# 原来这份清单和 requirements.txt 各写一份，两边靠人肉同步，已经漂了：requirements.txt
# 里有 bpy/imp/TodoService 等 11 个 PyPI 上不存在的名字（pip install 直接失败），
# 而这份里补了 uvloop/httptools/python-multipart 却没回填。现在平台差异改用
# PEP 508 环境标记（uvloop/httptools 标 sys_platform != "win32"）表达，一份就够。
#
# 保留这个文件名只是不让已部署实例的脚本和文档断链。
-r requirements.txt
