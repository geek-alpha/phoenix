# 基因 fitness：从「曝光」到「价值信号」设计（2026-09-14）

## 0. 一句话

已通电的部分只回答「这条基因进过几次 prompt」；本文设计的是「它进 prompt 之后，有没有改变结局」。

## 1. 现状：哪一半通了，哪一半还空着

| 环节 | 状态 | 证据 |
| --- | --- | --- |
| 候选池 | 有 | `harness_task_memory.json` 61 条教训 / `conviction.json` 2 条信条 / `long_horizon.json` 3 条事业 |
| 评估器（读出端） | 有 | `tools/gene_fitness.py` 报表：库大小、注入窗口、实际在场、曝光为 0 条数 |
| 选择压力 | 有 | `agent.py:1877 _gene_pick`：前 2 条新近位 + 其余按曝光升序补足 |
| 曝光埋点 | 有 | `agent.py:1862 _gene_touch` → `gene_stats.json`（累计计数） |
| **价值信号** | **无** | `gene_stats.json` 只存 `inject` 累计值，不存「哪一轮」；无法与任何结局关联 |

实测读数（2026-09-14 第 99 轮）：教训 61 条、窗口 6 条、**曝光为 0 仍有 45 条**（上一轮 49，每轮 −4~5）。轮换在走，但这只证明「在轮换」，不证明「轮进来的那 4 条有用」。

## 2. 为什么「曝光」不是 fitness

曝光是**自变量**（我们控制的：谁进窗口），价值是**因变量**（我们想知道的：进窗口有没有用）。把自变量当分数排序，等于「谁上过场谁继续上场」——上一轮已因此否掉过一次方案（见教训库「给学习机制加选择压力时，别拿曝光次数当 fitness 排序」）。

真正要回答的问题只有一句：**这条基因在场的那一轮，结局比它不在场时更好吗？**

## 3. 信号设计

### 3.1 自变量：轮次 × 基因键（当前缺的那块）

现在 `_gene_touch` 只累加计数。要能关联，必须记下**轮次**。落盘不进 `turn_metrics.jsonl`——它 `MAX_LINES=500 / KEEP_LINES=300`（`turn_metrics.py:30-31`）会滚动截断，历史会被吃掉；基因曝光需要长期留存，单独一个文件：

`data/gene_exposure.jsonl`，一行一轮：

```json
{"ts": 1789338524.1, "turn": "a1b2c3d4", "keys": ["lesson:8752d51ca6c2", "conviction:daf5d5fc0479", "..."]}
```

- `turn`：`agent.py` 已有的 `turn_id`（`agent.py:154`），无则用 `ts` 秒级时间戳
- `keys`：本轮真正进 prompt 的基因键（`kind:sha1[:12]`，键算法复用 `tools/gene_fitness.py:41 key_of`，不另写一份）

**不写回 `gene_stats.json`**：那是累计台账（一份真相），曝光流水是事件流（另一份真相），两者混在一个文件里，滚动/重写会互相踩。

### 3.2 因变量：按可信度从高到低

**A. 复发率（最硬，推荐主口径）**

多数教训描述的是**一个具体错误模式**（如「管道尾命令吞掉退出码」「按曝光降序排会锁死窗口」）。可测的定义：

> 教训 L 在场后，L 描述的错误模式在之后 N 轮里是否再次出现？

- 需要每条教训带一个「检测器」：正则或关键词（`tools/gene_fitness.py` 只读文本，不猜语义）
- 复发率 = 出现次数 / 该教训在场轮数；与「该教训不在场时的同模式发生率」对比
- 优点：不依赖 LLM 打分，可复算，可证伪
- 缺点：需要标注检测器（P2 期，先只对高频错误模式做）

**B. 该轮结局标签（中等，P1 可采）**

`turn_metrics.jsonl` 已有：`tool_errors`、`re_reads`、`truncations`、`duration_ms`（`agent.py:4323-4333`）。按 `ts` 与曝光流水对齐即可算「在场轮 vs 不在场轮」的均值差。

诚实标注：**这不是因果**。基因在场可能只是「那轮任务本来就难」的结果，也可能与结局无关。它只能当**证伪工具**——若某条基因在场轮的 `tool_errors` 显著高于基线，值得人工看一眼；不能据此判定「这条基因有害」。

**C. 用户纠正标记（弱，仅作旁证）**

下一轮用户消息命中纠正词（「不对 / 错了 / 不是 / 别 / 你搞错 / 重新」）记 1。启发式，必然有假阳性（「不对」也可能出现在肯定句里）。仅用于排序参考，不单独作为淘汰依据。

## 4. 判据：什么条件下才允许动一条基因

淘汰/降级一条基因需要**同时**满足：

1. 样本量门槛：该基因在场 ≥ 20 轮，且其检测器可复算（不靠感觉）
2. 有对照：在场轮 vs 不在场轮的同一指标差，方向一致且不落在噪声内
3. 人工复核：报表能打印出「支持淘汰」的具体轮次与原始记录，主人过一眼才执行

不满足任一条 → 保持现状。信条已明确：**「测不了」不等于「没用」**，拿没埋点当删除依据和拿感觉删除一样不诚实。

## 5. 分期实施

### P0（下一步原子动作）：曝光流水落盘

- 改 `agent.py:1862 _gene_touch`：除累计计数外，把 `(turn, keys)` 追加到 `data/gene_exposure.jsonl`
- 轮次上下文：主循环每轮开头设一个模块级 `_CURRENT_TURN`（`turn_id` 或秒级 ts），`_gene_touch` 读它；取不到就写 `"unknown"`，不阻断
- 键格式统一为 `kind:sha1[:12]`，复用 `key_of`，不新写一份（两份实现必然漂移，这是已踩过的坑）
- **验收**：跑一轮真实对话 → 文件出现 1 行 → 该行 `keys` 集合与 `gene_fitness.py` 报表的「在场」集合**逐条相等**（不一致就是口径漂移）
- 成本：约 20 行代码 + 3 个测试用例

### P1：结局对齐报表

- `tools/gene_fitness.py` 新增一节：按 `ts` 把曝光流水与 `turn_metrics.jsonl` 对齐，输出每条基因的「在场轮 tool_errors 均值 / 不在场轮均值 / 样本量」
- 样本量 < 20 轮的行**不出结论**，只打印原始计数（避免用 3 个样本下判断）
- **验收**：报表新增列不改变现有各节输出；现有 188 个测试全绿

### P2：复发检测器（最硬的那个信号）

- 先给 3~5 条高频错误模式教训加检测器（正则/关键词），存在教训条目里，如 `{"check": "py_compile.*\\|\\s*tail"}`
- 报表输出复发率与对照基线
- **验收**：手工构造「已复发 / 未复发」两组假数据，报表结论与构造一致（正反两面都测）

## 6. 已知局限（写在前面，别自我欺骗）

- **相关不是因果**：基因在场轮的结局，混杂了任务难度、模型版本、上下文长度。报表只能给出「值得看一眼」的线索，不能给出「这条基因有用」的结论。
- **曝光不等于阅读**：基因在 prompt 里 ≠ 模型真的读了它。这是本设计无法消除的上限。
- **检测器会腐坏**：教训文本改了、检测器没跟着改，复发率会静默归零——正是本次要修的故障本身（埋点造好没通电）。检测器必须和教训文本存在同一条目里，改文本就一起改。
- **样本攒得慢**：每轮最多 6 条教训在场，一条基因要攒 20 轮在场，按当前节奏需数十轮。P0 落地后要等，不能提前下结论。

## 7. 明确不做的事

- 不把 fitness 分数自动写回注入排序（自动淘汰基因）——淘汰是不可逆决策，先出报表，人拍板
- 不用 LLM 给教训打分当 fitness（不可复算、不可证伪，等于换个方式靠感觉）
- 不把曝光流水塞进 `turn_metrics.jsonl`（滚动截断会吃掉历史）
- 不因为「曝光为 0」就删基因（那是自变量，不是价值）
